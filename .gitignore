# Build / IDE / OS
.vscode
.idea
.DS_Store
captive-core*/
__debug_bin*
*.out

# Environment / secrets
.env
env.sh

# ── Claude Code — local-only (testing, not tracked) ──
.claude/
CLAUDE.md

# Serena language server state
.serena

# ── Arscontexta plugin stray files ──
/.arscontexta
/ops/

# ── Legacy knowledge path (moved to .claude/knowledge/) ──
docs/knowledge/
