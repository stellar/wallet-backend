interface Balance {
    account: Account! @goField(forceResolver: true)
    balance: String!
    tokenId: String!
}

type TrustlineBalance implements Balance {
    account: Account! @goField(forceResolver: true)
    balance: String!
    tokenId: String!

    code: String
    issuer: String
}
